<Window x:Class="BatchFileRenamer.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:BatchFileRenamer.Converters"
        mc:Ignorable="d"
        Title="Lịch Sử Đổi Tên &amp; Hoàn Tác (History &amp; Undo)" 
        Height="620" Width="980"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A"
        Foreground="#F8FAFC"
        FontFamily="Segoe UI, Tahoma, Arial"
        FontSize="13">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanConverter x:Key="InverseBool"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#475569"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0EA5E9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#991B1B"/>
            <Setter Property="BorderBrush" Value="#DC2626"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#3B82F6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="📜 Lịch Sử Đổi Tên &amp; Khôi Phục" FontSize="20" FontWeight="Bold" Foreground="#F8FAFC"/>
                <TextBlock Text="Xem lại các phiên làm việc và hoàn tác an toàn về tên ban đầu bất kỳ lúc nào." Foreground="#94A3B8" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="🔄 Làm mới" Command="{Binding LoadCommand}" Style="{StaticResource ModernButton}" Margin="0,0,8,0"/>
                <Button Content="🗑️ Xóa lịch sử" Command="{Binding ClearHistoryCommand}" Style="{StaticResource DangerButton}"/>
            </StackPanel>
        </Grid>

        <!-- Content Area: Left list of sessions, Right details of mappings -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sessions List -->
            <Border Grid.Column="0" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="DANH SÁCH CÁC PHIÊN" FontWeight="Bold" Foreground="#94A3B8" FontSize="11" Margin="4,0,0,8"/>
                    
                    <ListBox Grid.Row="1" ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}"
                             Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Margin" Value="0,0,0,6"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Background" Value="#0F172A"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#334155" BorderThickness="1" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#1E293B"/>
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="#64748B"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#1E3A8A"/>
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="#3B82F6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                        <TextBlock Text="⏰ " Foreground="#38BDF8"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:dd/MM/yyyy HH:mm:ss}'}" FontWeight="Bold" Foreground="#F8FAFC"/>
                                    </StackPanel>

                                    <TextBlock Grid.Row="1" Text="{Binding DirectoryPath}" Foreground="#94A3B8" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>

                                    <Grid Grid.Row="2" Margin="0,5,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding SuccessCount, StringFormat='Số tệp: {0}'}" Foreground="#E2E8F0" FontSize="11"/>
                                        
                                        <!-- Rolled back badge -->
                                        <Border Grid.Column="1" CornerRadius="4" Padding="6,2" Background="#78350F" Visibility="{Binding IsRolledBack, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="Đã hoàn tác" Foreground="#FCD34D" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Session Detail / Mapping List -->
            <Border Grid.Column="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="CHI TIẾT ÁNH XẠ ĐỔI TÊN" FontWeight="Bold" Foreground="#94A3B8" FontSize="11"/>
                            <TextBlock Text="{Binding SelectedSession.DirectoryPath, StringFormat='Thư mục: {0}'}" Foreground="#CBD5E1" FontSize="12" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <!-- Undo Action Button -->
                        <Button Grid.Column="1" Content="↩️ Hoàn tác phiên này" 
                                Command="{Binding UndoCommand}" 
                                Style="{StaticResource PrimaryActionButton}"
                                IsEnabled="{Binding CanUndo}"/>
                    </Grid>

                    <!-- DataGrid of mappings -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedSession.Mappings}" AutoGenerateColumns="False"
                              IsReadOnly="True" Background="#0F172A" BorderBrush="#334155" RowBackground="#0F172A" AlternatingRowBackground="#172033"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#334155" HeadersVisibility="Column"
                              Foreground="#F8FAFC">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#1E293B"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderBrush" Value="#334155"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên Cũ (Ban đầu)" Binding="{Binding OriginalFullPath}" Width="*"/>
                            <DataGridTextColumn Header="Tên Mới (Đã đổi)" Binding="{Binding NewFullPath}" Width="*"/>
                            <DataGridTemplateColumn Header="Trạng thái" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#064E3B" CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                            <TextBlock Text="Thành công" Foreground="#34D399" FontSize="11" FontWeight="SemiBold"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Status message -->
                    <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="#38BDF8" Margin="0,10,0,0" FontWeight="SemiBold"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Margin="0,14,0,0" BorderBrush="#334155" BorderThickness="0,1,0,0" Padding="0,10,0,0">
            <Grid>
                <TextBlock Text="💡 Gợi ý: Chỉ các phiên chưa từng bị hoàn tác và các tệp còn giữ nguyên vị trí mới có thể hoàn tác an toàn." Foreground="#94A3B8" VerticalAlignment="Center"/>
                <Button Content="Đóng" Click="CloseButton_Click" HorizontalAlignment="Right" Style="{StaticResource ModernButton}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
